# goreleaser config for the axilio CLI (schema v2).
# Local check:   goreleaser check
# Dry-run build: goreleaser release --snapshot --clean
# Real release:  bump VERSION in your PR; merging runs the "Publish CLI"
#                workflow (tag + goreleaser). See .github/workflows/publish.yml.
version: 2

project_name: axilio

before:
  hooks:
    - go mod tidy
    # Fail the release if command metadata changed without regenerating the
    # checked-in page, or if mandoc finds even a non-fatal diagnostic.
    - sh scripts/check_manpage.sh

builds:
  - id: axilio
    main: ./cmd/axilio
    binary: axilio
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X github.com/axilioai/cli/cmd.Version={{ .Version }}
      - -X github.com/axilioai/cli/cmd.Commit={{ .FullCommit }}
      - -X github.com/axilioai/cli/cmd.Date={{ .Date }}

archives:
  - id: axilio
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    # A non-empty files list replaces GoReleaser's implicit README/license
    # globs, so keep README explicit alongside the generated manual.
    files:
      - README.md
      - man/axilio.1

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  draft: false
  prerelease: auto
  # A workflow rerun may need to repair a stale cask after the GitHub release
  # was already created. Rebuild and replace only GoReleaser-owned asset names.
  replace_existing_artifacts: true

# Homebrew tap (macOS). Publishes the axilio cask to axilioai/homebrew-tap on
# each non-prerelease tag — goreleaser deprecated formula `brews` in favor of
# casks. Requires the tap repo to exist and a HOMEBREW_TAP_TOKEN secret (PAT with
# contents:write on the tap) set on this repo. skip_upload: auto skips the push
# for prereleases (e.g. v0.1.0-rc1), so an -rc tag validates the pipeline without
# needing the token. Linux users install via `go install` / the curl script.
homebrew_casks:
  - name: axilio
    binaries:
      - axilio
    manpages:
      - man/axilio.1
    repository:
      owner: axilioai
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://axilio.ai"
    description: "Acquire and drive Axilio phones from the command line."
    commit_author:
      name: axilio-bot
      email: bot@axilio.ai
    skip_upload: auto
    # The release binary is unsigned; strip the macOS quarantine attribute so it
    # runs without a Gatekeeper prompt on first launch.
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/axilio"]
          end
